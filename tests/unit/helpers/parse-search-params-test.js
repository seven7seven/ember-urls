import { parseSearchParams } from 'ember-urls/helpers/parse-search-params';
import { module, test } from 'qunit';

module('Unit | Helper | parse search params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseSearchParams([42]);
  assert.ok(result);
});
